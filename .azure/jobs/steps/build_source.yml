parameters:
  example: 'foo'

steps:
- template: setup_workspace.yml
  parameters:
    key: overlay
    underlay: /opt/underlay_ws
    workspace: /opt/overlay_ws
    mixins: ${OVERLAY_MIXINS}
- template: ccache_stats.yml
  parameters:
    name: ccache_stats_overlay
# - template: store_ccache_logs.yml
#   parameters:
#     foo
# - template: cache_with_workspace.yml
#   parameters:
#     key: ccache
#     workspace: /opt/underlay_ws
#     path: /root/ccache
