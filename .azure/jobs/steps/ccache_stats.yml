parameters:
  name: ''

steps:
- script: |
    set -x
    ccache -s # show stats
    ccache -z # zero stats
    ccache -V # show version
    ccache -p # show config
  displayName: CCache Stats
  name: ${{ parameters.name }}
