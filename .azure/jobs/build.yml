parameters:
  name: 'Build'
  pool: 'Default'
  container: ''

jobs:
- job: ${{ parameters.name }}
  pool: ${{ parameters.pool }}
  container: ${{ parameters.container }}
  steps:
  - template: steps/checkout_source.yml
  - template: steps/setup_dependencies.yml
  - template: steps/build_source.yml
